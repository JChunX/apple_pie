defaults:
  - _self_
  - agent: ppo_agent
  - environment: so100_peg_insert_env
  - models: so100_rgb_actor_critic
  - trainer: default_sequential_trainer


wandb:
  entity: "jchunx"
  project: "so100_peg_insert_ppo_train"

device: cuda:0
mode: training